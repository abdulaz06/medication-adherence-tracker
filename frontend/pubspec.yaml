name: medication_adherence_tracker
description: Medication & Supplement Adherence Tracker

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  intl: ^0.19.0
  shared_preferences: ^2.3.0
  fl_chart: ^0.70.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
